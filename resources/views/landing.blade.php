@php
    $locale = $locale ?? 'id';
    $t = [
        'meta_title' => [
            'id' => 'MarketplaceJamaah — Jual Beli Komunitas WhatsApp',
            'en' => 'MarketplaceJamaah — Buy & Sell via WhatsApp Community',
        ],
        'meta_desc' => [
            'id' => 'Temukan ribuan produk dari komunitas jamaah WhatsApp. Jual beli mudah, aman, dan terpercaya.',
            'en' => 'Discover products from the WhatsApp jamaah community. Easy, trusted buying and selling.',
        ],
        'brand_sub'  => ['id' => 'Marketplace WhatsApp Bertenaga AI', 'en' => 'AI-powered WhatsApp Marketplace'],
        'nav_guide'  => ['id' => 'Panduan', 'en' => 'Guide'],
        'nav_feat'   => ['id' => 'Fitur',   'en' => 'Features'],
        'nav_dash'   => ['id' => 'Dashboard', 'en' => 'Dashboard'],

        'hero_eyebrow'=> ['id' => 'AI · Marketplace WhatsApp', 'en' => 'AI · WhatsApp Marketplace'],
        'hero_h1_a'   => ['id' => 'Semua produk',              'en' => 'All products from'],
        'hero_h1_b'   => ['id' => 'komunitas jamaah',          'en' => 'the jamaah community'],
        'hero_h1_c'   => ['id' => 'dalam satu tempat.',        'en' => 'in one place.'],
        'hero_lead'   => [
            'id' => 'Produk dari grup WhatsApp jamaah dideteksi & dikurasi otomatis oleh AI — mudah, cepat, dan terpercaya.',
            'en' => 'Products from WhatsApp jamaah groups are auto-detected & curated by AI — easy, fast, trustworthy.',
        ],
        'stat_active' => ['id' => 'produk aktif', 'en' => 'active listings'],
        'stat_cat'    => ['id' => 'kategori',     'en' => 'categories'],
        'stat_wa'     => ['id' => 'Via WhatsApp', 'en' => 'Via WhatsApp'],
        'cta_browse'  => ['id' => 'Jelajahi Produk', 'en' => 'Browse Products'],
        'cta_join'    => ['id' => 'Join Grup',       'en' => 'Join Group'],

        'search_ph'   => ['id' => 'Cari nama produk, deskripsi…', 'en' => 'Search product name, description…'],
        'btn_search'  => ['id' => 'Cari', 'en' => 'Search'],
        'btn_filter_hint' => ['id' => 'Filter harga & lokasi', 'en' => 'Price & location filter'],
        'filter_minp' => ['id' => 'Harga Min (Rp)', 'en' => 'Min Price (Rp)'],
        'filter_maxp' => ['id' => 'Harga Max (Rp)', 'en' => 'Max Price (Rp)'],
        'filter_loc'  => ['id' => 'Lokasi Penjual', 'en' => 'Seller Location'],
        'filter_loc_ph' => ['id' => 'contoh: Bekasi, Jakarta Selatan…', 'en' => 'e.g.: Bekasi, South Jakarta…'],
        'cat_all'     => ['id' => 'Semua', 'en' => 'All'],
        'btn_reset'   => ['id' => 'Reset semua filter', 'en' => 'Reset all filters'],

        'trust_verified' => ['id' => 'Penjual Terverifikasi', 'en' => 'Verified Sellers'],
        'trust_wa'       => ['id' => 'Hubungi via WhatsApp', 'en' => 'Contact via WhatsApp'],
        'trust_ai'       => ['id' => 'Dikurasi AI Otomatis', 'en' => 'AI-Curated Automatically'],
        'trust_comm'     => ['id' => 'Komunitas Jamaah', 'en' => 'Jamaah Community'],

        'top_cat_title' => ['id' => 'Kategori Terpopuler', 'en' => 'Most Popular Categories'],
        'top_cat_sub'   => ['id' => 'berdasarkan jumlah produk aktif', 'en' => 'by number of active listings'],

        'wa_label' => ['id' => 'Komunitas WhatsApp Jamaah', 'en' => 'WhatsApp Jamaah Community'],
        'wa_title' => ['id' => 'Gabung Grup WhatsApp Kami Sekarang!', 'en' => 'Join Our WhatsApp Group Now!'],
        'wa_desc'  => [
            'id' => 'Scan QR code atau klik tombol di bawah untuk bergabung. Jual produkmu, temukan pembeli, dan dapatkan penawaran terbaik dari komunitas jamaah.',
            'en' => 'Scan the QR or tap below to join. Sell your products, find buyers, and discover offers from the jamaah community.',
        ],
        'wa_btn' => ['id' => 'Join Grup WhatsApp', 'en' => 'Join WhatsApp Group'],

        'mod_title' => ['id' => 'Produk belum muncul?', 'en' => 'Listing not showing yet?'],
        'mod_body' => [
            'id' => 'Jika kamu baru saja posting iklan di grup WhatsApp, iklanmu sedang dalam proses moderasi AI dan akan tampil otomatis dalam beberapa menit. Pastikan iklan sudah menggunakan format lengkap (nama + harga + deskripsi + foto).',
            'en' => 'If you just posted in the WhatsApp group, your listing is being moderated by AI and will appear automatically in a few minutes. Make sure the post has name + price + description + photo.',
        ],

        'section_media_title' => ['id' => 'Produk dengan Foto / Video', 'en' => 'Products with Photo / Video'],
        'section_media_sub'   => ['id' => 'produk', 'en' => 'items'],
        'section_text_title'  => ['id' => 'Iklan Baris', 'en' => 'Classified Ads'],
        'section_text_sub'    => ['id' => 'iklan · tanpa foto', 'en' => 'ads · text only'],
        'section_text_lead'   => ['id' => 'Iklan teks tanpa foto — klik untuk detail, atau hubungi penjual langsung via WhatsApp.', 'en' => 'Text-only ads — tap for details or contact the seller via WhatsApp.'],

        'empty_title' => ['id' => 'Produk tidak ditemukan', 'en' => 'No products found'],
        'empty_sub'   => ['id' => 'Coba kata kunci lain atau pilih kategori berbeda', 'en' => 'Try different keywords or another category'],
        'empty_btn'   => ['id' => 'Lihat Semua Produk', 'en' => 'See All Products'],

        'load_more_media' => ['id' => 'Muat Lebih Banyak Produk', 'en' => 'Load More Products'],
        'load_more_text'  => ['id' => 'Muat Lebih Banyak Iklan', 'en' => 'Load More Ads'],
        'loading'         => ['id' => 'Memuat…', 'en' => 'Loading…'],

        'footer_tag' => [
            'id' => 'Platform jual beli otomatis dari grup WhatsApp jamaah',
            'en' => 'Automated marketplace from WhatsApp jamaah groups',
        ],

        'whatsapp_seller_msg' => [
            'id' => 'Halo, saya tertarik dengan produk "%s". Apakah masih tersedia?',
            'en' => 'Hi, I\'m interested in the "%s" listing. Is it still available?',
        ],
        'hero_contact' => ['id' => 'Hubungi Penjual', 'en' => 'Contact Seller'],
        'hero_price_nego' => ['id' => 'Harga nego', 'en' => 'Negotiable'],
        'seller_word' => ['id' => 'Penjual', 'en' => 'Seller'],
    ];
    $tr = fn($k) => $t[$k][$locale] ?? $t[$k]['id'];
    $langToggleUrl = function($target) {
        $qs = array_merge(request()->query(), ['lang' => $target]);
        return url()->current() . (count($qs) ? '?' . http_build_query($qs) : '');
    };
@endphp
<!DOCTYPE html>
<html lang="{{ $locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>{{ $tr('meta_title') }}</title>
    <meta name="description" content="{{ $tr('meta_desc') }}">
    <link rel="canonical" href="{{ url('/') }}">
    <link rel="alternate" hreflang="id" href="{{ url('/') }}?lang=id">
    <link rel="alternate" hreflang="en" href="{{ url('/') }}?lang=en">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root{
            --em:#059669; --em-dark:#047857; --em-mid:#34d399; --em-light:#a7f3d0; --em-xlight:#ecfdf5;
            --ink:#0b1220; --ink-soft:#1f2937; --muted:#6b7280; --hair:#e5e7eb;
            --amber:#f59e0b; --amber-dark:#d97706; --amber-light:#fef3c7;
            --radius-lg:22px; --radius-md:16px; --radius-sm:12px;
            --shadow-soft:0 2px 14px rgba(15,23,42,.05),0 1px 3px rgba(15,23,42,.04);
            --shadow-hover:0 22px 50px -14px rgba(5,150,105,.2),0 8px 20px rgba(15,23,42,.08);
        }
        *{box-sizing:border-box;}
        html{scroll-behavior:smooth;}
        body{
            margin:0; color:var(--ink);
            font-family:'Plus Jakarta Sans','Segoe UI',system-ui,sans-serif;
            background:
                radial-gradient(1200px 600px at 85% -10%, rgba(52,211,153,.18), transparent 60%),
                radial-gradient(900px 500px at -10% 25%, rgba(110,231,183,.12), transparent 60%),
                linear-gradient(180deg,#f7fdfa 0%,#f0fdf8 100%);
            overflow-x:hidden;
        }
        .container-xl{max-width:1200px;}

        /* ── Floating Nav ─────────────────────────── */
        .nav-wrap{position:sticky; top:0; z-index:100; padding:.55rem 0;}
        .nav-bar{
            background:rgba(255,255,255,.75); backdrop-filter:saturate(1.6) blur(18px);
            -webkit-backdrop-filter:saturate(1.6) blur(18px);
            border:1px solid rgba(15,23,42,.06); border-radius:18px; padding:.55rem .9rem;
            box-shadow:0 10px 28px -10px rgba(5,150,105,.18), 0 2px 6px rgba(15,23,42,.04);
            display:flex; align-items:center; justify-content:space-between; gap:.75rem;
        }
        .brand{display:flex; align-items:center; gap:.6rem; text-decoration:none;}
        .brand-logo{
            width:40px; height:40px; border-radius:12px;
            background:linear-gradient(135deg,#059669 0%,#10b981 55%,#34d399 100%);
            display:grid; place-items:center; box-shadow:0 6px 16px rgba(5,150,105,.4);
        }
        .brand-logo i{color:#fff; font-size:1.1rem;}
        .brand-name{font-weight:800; color:var(--ink); font-size:1.02rem; line-height:1.1;}
        .brand-name span{color:var(--em);}
        .brand-sub{font-size:.65rem; color:var(--muted); font-weight:600;}
        .nav-actions{display:flex; gap:.4rem; align-items:center;}
        .btn-ghost{
            background:#fff; color:#111; border:1px solid var(--hair);
            padding:.45rem .9rem; border-radius:12px; font-size:.82rem; font-weight:700;
            display:inline-flex; align-items:center; gap:.35rem;
            text-decoration:none; transition:all .18s; white-space:nowrap;
        }
        .btn-ghost:hover{border-color:var(--em); color:var(--em); transform:translateY(-1px);}
        .btn-solid{
            background:linear-gradient(135deg,#059669,#10b981);
            color:#fff; border:none; padding:.5rem 1rem; border-radius:12px;
            font-weight:700; font-size:.82rem; display:inline-flex; align-items:center; gap:.4rem;
            text-decoration:none; box-shadow:0 6px 18px rgba(5,150,105,.38); transition:transform .18s;
            white-space:nowrap;
        }
        .btn-solid:hover{color:#fff; transform:translateY(-2px);}
        .lang-pill{
            display:inline-flex; padding:3px; background:#f1f5f9; border-radius:999px;
            border:1px solid var(--hair);
        }
        .lang-pill a{
            padding:.25rem .65rem; border-radius:999px; font-size:.72rem; font-weight:800;
            color:#475569; text-decoration:none; letter-spacing:.04em; transition:all .18s;
        }
        .lang-pill a.on{background:#0b1220; color:#fff; box-shadow:0 4px 12px rgba(11,18,32,.22);}
        .lang-pill a:hover:not(.on){color:#111;}

        /* ── Hero ── (clean, no chaotic bg image) ── */
        .hero{position:relative; padding:3.5rem 0 4rem;}
        .hero::before{
            content:""; position:absolute; inset:0; z-index:0;
            background:
                radial-gradient(620px 320px at 12% 25%, rgba(52,211,153,.22), transparent 60%),
                radial-gradient(520px 300px at 92% 15%, rgba(59,130,246,.08), transparent 60%);
            pointer-events:none;
        }
        .hero-grid{
            position:relative; z-index:1;
            display:grid; grid-template-columns:1fr; gap:2.5rem; align-items:center;
        }
        @media(min-width:992px){
            .hero-grid{grid-template-columns:1.15fr 1fr; gap:3rem;}
        }
        .hero-eyebrow{
            display:inline-flex; align-items:center; gap:.42rem;
            background:rgba(16,185,129,.1); border:1px solid rgba(16,185,129,.28);
            color:var(--em-dark); border-radius:999px; padding:.32rem 1rem;
            font-size:.72rem; font-weight:800; letter-spacing:.08em; text-transform:uppercase;
        }
        .hero h1{
            font-size:clamp(1.9rem, 3.2vw + 1rem, 3.6rem);
            font-weight:900; line-height:1.08; letter-spacing:-.02em;
            margin:1rem 0 1rem; color:var(--ink);
            overflow-wrap:break-word;
        }
        .hero h1 em{
            font-style:normal;
            background:linear-gradient(90deg,#059669,#10b981 55%,#34d399);
            -webkit-background-clip:text; background-clip:text; color:transparent;
            display:inline-block;
        }
        .hero p.lead{font-size:clamp(.96rem,.6vw + .85rem,1.1rem); color:#334155; max-width:540px; line-height:1.7; margin:0 0 1.4rem;}
        .hero-stats{display:flex; flex-wrap:wrap; gap:.5rem; margin-bottom:1.3rem;}
        .hero-chip{
            display:inline-flex; align-items:center; gap:.35rem;
            background:#fff; border:1px solid var(--hair); border-radius:999px;
            padding:.35rem .9rem; font-size:.8rem; font-weight:700; color:var(--ink-soft);
            box-shadow:var(--shadow-soft);
        }
        .hero-chip i{color:var(--em);}
        .hero-ctas{display:flex; gap:.55rem; flex-wrap:wrap;}

        /* Hero: floating product card(s) ── */
        .hero-stage{
            position:relative; min-height:420px;
        }
        .hero-card{
            position:relative; background:#fff; border:1px solid var(--hair);
            border-radius:22px; overflow:hidden; box-shadow:0 30px 70px -20px rgba(5,150,105,.28), 0 8px 22px rgba(15,23,42,.08);
            max-width:100%;
        }
        .hero-card.alt{
            position:absolute; top:6%; left:-1.5rem; width:72%;
            transform:rotate(-3.2deg);
            z-index:1; opacity:.85;
        }
        .hero-card.main{
            position:relative; margin-left:auto; width:90%;
            transform:rotate(2deg);
            z-index:2;
        }
        @media(max-width:991px){
            .hero-stage{min-height:340px;}
            .hero-card.alt{display:none;}
            .hero-card.main{margin:0 auto; width:100%; max-width:360px; transform:rotate(0);}
        }
        .hero-card .media{
            width:100%; aspect-ratio:16/10; object-fit:cover; display:block;
            background:linear-gradient(135deg,#ecfdf5,#d1fae5);
        }
        .hero-card .body{padding:.9rem 1rem 1.1rem;}
        .hero-card .cat{
            display:inline-block; font-size:.62rem; font-weight:800; letter-spacing:.07em; text-transform:uppercase;
            color:var(--em-dark); background:var(--em-xlight); border:1px solid var(--em-light);
            border-radius:6px; padding:.15rem .55rem; margin-bottom:.4rem;
        }
        .hero-card h4{font-size:1rem; font-weight:800; margin:0; line-height:1.35; color:var(--ink);
            overflow:hidden; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical;
        }
        .hero-card .price{font-size:1.15rem; font-weight:800; margin:.35rem 0;
            background:linear-gradient(90deg,#059669,#10b981); -webkit-background-clip:text; background-clip:text; color:transparent;
        }
        .hero-card .meta{font-size:.74rem; color:var(--muted); display:flex; align-items:center; gap:.3rem;}
        .hero-card .wa{
            display:inline-flex; align-items:center; gap:.4rem; margin-top:.7rem;
            background:linear-gradient(135deg,#059669,#10b981); color:#fff;
            padding:.48rem 1rem; border-radius:10px; font-size:.82rem; font-weight:800;
            text-decoration:none; box-shadow:0 4px 12px rgba(5,150,105,.35);
        }
        .hero-card .wa:hover{color:#fff;}
        .hero-pulse{
            position:absolute; top:-14px; right:-14px; z-index:3;
            background:#fff; border:1px solid var(--em-light); border-radius:999px;
            padding:.3rem .75rem; font-size:.7rem; font-weight:800; color:var(--em-dark);
            box-shadow:0 8px 20px rgba(5,150,105,.18);
            display:inline-flex; align-items:center; gap:.3rem;
        }
        .hero-pulse::before{
            content:""; width:7px; height:7px; border-radius:50%; background:var(--em);
            box-shadow:0 0 0 0 rgba(5,150,105,.45); animation:pulse 1.8s infinite;
        }
        @keyframes pulse{
            0%{box-shadow:0 0 0 0 rgba(5,150,105,.5);}
            70%{box-shadow:0 0 0 10px rgba(5,150,105,0);}
            100%{box-shadow:0 0 0 0 rgba(5,150,105,0);}
        }

        /* ── Search card (floating) ── */
        .search-card{
            background:#fff; border:1px solid var(--hair); border-radius:var(--radius-lg);
            box-shadow:0 12px 40px -12px rgba(5,150,105,.18), 0 2px 8px rgba(0,0,0,.05);
            padding:1.3rem 1.4rem; margin-top:.5rem;
            position:relative; z-index:5;
        }
        .search-card .form-control, .search-card .input-group-text{
            border-color:#e5e7eb; font-size:.92rem; color:var(--ink);
        }
        .search-card .form-control:focus{border-color:var(--em); box-shadow:0 0 0 3px rgba(5,150,105,.15);}
        .btn-search{
            background:linear-gradient(135deg,#059669,#10b981);
            color:#fff; border:none; border-radius:12px;
            font-weight:800; padding:.58rem 1.6rem; font-size:.88rem;
            box-shadow:0 5px 14px rgba(5,150,105,.38);
            transition:all .18s; white-space:nowrap;
            display:inline-flex; align-items:center; gap:.35rem;
        }
        .btn-search:hover{transform:translateY(-2px); color:#fff;}
        .btn-reset{
            background:#fff; border:1px solid var(--hair); color:var(--muted);
            border-radius:12px; font-weight:700; font-size:.85rem;
            padding:.5rem .95rem; transition:all .15s; white-space:nowrap;
            display:inline-flex; align-items:center; gap:.3rem;
        }
        .btn-reset:hover{border-color:var(--em); color:var(--em);}

        /* ── Category pills ── */
        .cat-pill{
            display:inline-flex; align-items:center; gap:.3rem;
            padding:.4rem .95rem; border-radius:999px;
            background:#f8fafc; color:#475569; border:1px solid #e2e8f0;
            font-size:.8rem; font-weight:700; text-decoration:none; transition:all .15s; white-space:nowrap;
        }
        .cat-pill:hover{background:var(--em-xlight); color:var(--em); border-color:var(--em-light);}
        .cat-pill.active{
            background:linear-gradient(135deg,#059669,#10b981);
            color:#fff; border-color:transparent;
            box-shadow:0 4px 12px rgba(5,150,105,.35);
        }
        .cat-pills-wrap{display:flex; gap:.45rem; overflow-x:auto; padding:3px 1px; scrollbar-width:none;}
        .cat-pills-wrap::-webkit-scrollbar{display:none;}

        /* ── Trust strip ── */
        .trust-strip{
            background:#fff; border:1px solid var(--hair); border-radius:var(--radius-md);
            padding:1rem 1.25rem; box-shadow:var(--shadow-soft);
        }
        .trust-row{
            display:grid; grid-template-columns:repeat(auto-fit,minmax(170px,1fr));
            gap:.85rem;
        }
        .trust-item{
            display:flex; align-items:center; gap:.55rem;
            font-size:.84rem; color:var(--ink-soft); font-weight:700;
        }
        .trust-item i{
            font-size:1.15rem;
            background:linear-gradient(135deg,#059669,#34d399);
            -webkit-background-clip:text; background-clip:text; color:transparent;
        }

        /* ── Top categories ── */
        .topcat-head{display:flex; align-items:center; justify-content:space-between; gap:.5rem; margin-bottom:.7rem;}
        .topcat-head h6{margin:0; font-weight:800; font-size:.95rem; color:var(--ink); display:inline-flex; align-items:center; gap:.4rem;}
        .topcat-head h6 i{color:var(--em);}
        .topcat-head .muted{font-size:.76rem; color:var(--muted);}
        .topcat-row{display:flex; gap:.5rem; flex-wrap:wrap;}
        .topcat-item{
            display:inline-flex; align-items:center; gap:.55rem;
            border-radius:14px; padding:.5rem .95rem; font-weight:700; font-size:.85rem;
            text-decoration:none; color:#064e3b;
            border:1px solid transparent;
            box-shadow:var(--shadow-soft);
            transition:transform .15s, box-shadow .15s;
        }
        .topcat-item:hover{transform:translateY(-2px); color:#064e3b;}
        .topcat-item .rank{font-weight:900; opacity:.55;}
        .topcat-item .cnt{background:rgba(0,0,0,.08); border-radius:999px; padding:1px 8px; font-size:.72rem; font-weight:800;}

        /* ── WA Join banner ── */
        .wa-banner{
            background:linear-gradient(135deg,#022c22 0%,#065f46 50%,#10b981 100%);
            border-radius:24px; padding:1.6rem 1.8rem; color:#fff;
            box-shadow:0 24px 60px -16px rgba(5,150,105,.45), inset 0 1px 0 rgba(110,231,183,.15);
            position:relative; overflow:hidden;
        }
        .wa-banner::before{
            content:""; position:absolute; inset:auto -40px -60px auto; width:240px; height:240px;
            border-radius:50%; background:radial-gradient(circle,rgba(52,211,153,.35),transparent 65%);
        }
        .wa-inner{position:relative; z-index:1; display:grid; grid-template-columns:auto 1fr; gap:1.2rem; align-items:center;}
        @media(max-width:580px){ .wa-inner{grid-template-columns:1fr; gap:1rem; text-align:center;} .wa-banner{padding:1.4rem 1.2rem;} }
        .wa-qr{
            background:#fff; border-radius:14px; padding:10px;
            box-shadow:0 10px 24px rgba(0,0,0,.25);
        }
        .wa-lbl{font-size:.7rem; font-weight:800; letter-spacing:.08em; text-transform:uppercase; color:#6ee7b7; display:inline-flex; align-items:center; gap:.4rem; margin-bottom:.3rem;}
        .wa-title{font-weight:900; font-size:1.2rem; margin:0 0 .3rem; letter-spacing:-.01em;}
        .wa-desc{font-size:.88rem; color:rgba(255,255,255,.82); line-height:1.6; margin:0 0 .95rem;}
        .wa-btn{
            display:inline-flex; align-items:center; gap:.45rem;
            background:#fff; color:var(--em-dark); padding:.6rem 1.4rem; border-radius:12px;
            font-weight:800; font-size:.88rem; text-decoration:none;
            box-shadow:0 8px 20px rgba(0,0,0,.22); transition:transform .18s;
        }
        .wa-btn:hover{color:var(--em); transform:translateY(-2px);}

        /* ── Moderation notice ── */
        .mod-notice{
            background:#fffbeb; border:1px solid #fde68a; border-radius:14px;
            padding:.85rem 1.1rem; display:flex; align-items:flex-start; gap:.65rem;
        }
        .mod-notice .ico{font-size:1.2rem; flex-shrink:0;}
        .mod-notice .body{font-size:.82rem; color:#92400e; line-height:1.55;}

        /* ── Section divider ── */
        .section-divider{display:flex; align-items:center; gap:1rem; margin:2.8rem 0 1.2rem;}
        .section-divider h3{
            margin:0; font-size:1.05rem; font-weight:900; color:var(--ink);
            display:flex; align-items:center; gap:.5rem; white-space:nowrap;
        }
        .section-divider h3 i{color:var(--em);}
        .section-divider .count{font-size:.78rem; color:var(--muted); font-weight:600;}
        .section-divider .line{flex:1; height:1.5px; background:linear-gradient(90deg,#d1fae5,transparent);}

        /* ── Product cards (KEEP CLASS NAMES for _cards partial compatibility) ── */
        .product-card{
            background:#fff; border:1px solid var(--hair); border-radius:18px;
            overflow:hidden; transition:box-shadow .22s, transform .22s, border-color .22s;
            height:100%; display:flex; flex-direction:column;
            box-shadow:var(--shadow-soft);
        }
        .product-card:hover{box-shadow:var(--shadow-hover); transform:translateY(-4px); border-color:var(--em-mid);}
        .product-img-wrap{width:100%; position:relative; padding-top:75%; overflow:hidden; background:var(--em-xlight);}
        .product-img-wrap img{
            position:absolute; top:0; left:0; width:100%; height:100%;
            object-fit:cover; object-position:center; transition:transform .4s;
        }
        .product-card:hover .product-img-wrap img{transform:scale(1.08);}
        .product-img-placeholder{
            position:absolute; top:0; left:0; width:100%; height:100%;
            display:flex; align-items:center; justify-content:center;
            background:linear-gradient(135deg,#ecfdf5,#a7f3d0 50%,#d1fae5);
        }
        .product-img-placeholder i{font-size:3rem; color:var(--em-mid);}
        .product-body{padding:.9rem 1rem; flex:1; display:flex; flex-direction:column; gap:.25rem;}
        .product-category{
            font-size:.66rem; font-weight:800; letter-spacing:.05em; text-transform:uppercase;
            color:var(--em-dark); background:var(--em-xlight); border-radius:6px;
            padding:.2rem .55rem; display:inline-block; border:1px solid var(--em-light);
        }
        .product-title{
            font-size:.9rem; font-weight:800; color:var(--ink);
            overflow:hidden; display:-webkit-box; -webkit-line-clamp:2;
            -webkit-box-orient:vertical; line-height:1.4; margin-top:.15rem;
        }
        .product-price{
            font-size:1.08rem; font-weight:900;
            background:linear-gradient(90deg,#059669,#10b981);
            -webkit-background-clip:text; -webkit-text-fill-color:transparent;
            background-clip:text;
        }
        .product-price.negotiable{
            background:linear-gradient(90deg,#d97706,#f59e0b);
            -webkit-background-clip:text; -webkit-text-fill-color:transparent;
            background-clip:text;
        }
        .product-seller{
            font-size:.74rem; color:var(--muted);
            display:flex; align-items:center; gap:.3rem; margin-top:auto;
            padding-top:.5rem; border-top:1.5px dashed #d1fae5;
        }
        .product-seller i{color:var(--em-mid);}
        .product-footer{
            padding:.65rem .9rem;
            border-top:1px solid #f0fdf4;
            background:linear-gradient(135deg,#f0fdf4,#ecfdf5);
        }
        .btn-wa{
            background:linear-gradient(135deg,#059669,#10b981);
            color:#fff; border:none; border-radius:9px;
            font-size:.8rem; font-weight:800;
            padding:.5rem .9rem; text-decoration:none; display:inline-flex;
            align-items:center; gap:.35rem; width:100%; justify-content:center;
            transition:all .2s; box-shadow:0 3px 10px rgba(5,150,105,.32);
        }
        .btn-wa:hover{transform:translateY(-2px); box-shadow:0 6px 18px rgba(5,150,105,.48); color:#fff;}
        .video-play-badge{
            position:absolute; bottom:.5rem; left:.5rem; z-index:2;
            background:rgba(0,0,0,.6); color:#fff; border-radius:100px;
            padding:.2rem .6rem; font-size:.7rem; font-weight:800;
            display:flex; align-items:center; gap:.25rem;
            pointer-events:none;
        }
        .product-img-wrap video{
            position:absolute; top:0; left:0; width:100%; height:100%;
            object-fit:cover; object-position:center;
        }

        /* ── Iklan baris (compact list) ── */
        .iklan-baris-list{display:flex; flex-direction:column; gap:.55rem;}
        .iklan-baris-row{
            display:grid;
            grid-template-columns:minmax(0,1fr) auto auto;
            grid-template-areas:"main price wa";
            align-items:center;
            column-gap:1rem;
            background:#fff; border:1px solid var(--hair); border-radius:14px;
            padding:.75rem 1rem; text-decoration:none; color:inherit;
            transition:border-color .18s, box-shadow .18s, transform .18s;
        }
        .iklan-baris-row:hover{border-color:var(--em-mid); box-shadow:0 6px 20px rgba(5,150,105,.1); color:inherit; transform:translateY(-1px);}
        .iklan-baris-main{grid-area:main; min-width:0; display:flex; flex-direction:column; gap:.18rem;}
        .iklan-baris-cat{
            display:inline-block; align-self:flex-start;
            font-size:.6rem; font-weight:800; letter-spacing:.05em; text-transform:uppercase;
            color:var(--em-dark); background:var(--em-xlight); border:1px solid var(--em-light);
            border-radius:6px; padding:.14rem .5rem; white-space:nowrap; max-width:100%;
            overflow:hidden; text-overflow:ellipsis;
        }
        .iklan-baris-title{font-size:.9rem; font-weight:800; color:var(--ink); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; line-height:1.3;}
        .iklan-baris-desc{font-size:.75rem; color:var(--muted); white-space:nowrap; overflow:hidden; text-overflow:ellipsis;}
        .iklan-baris-meta{font-size:.7rem; color:#9ca3af; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; display:flex; align-items:center; gap:.35rem; margin-top:.12rem;}
        .iklan-baris-price{
            grid-area:price;
            font-size:.92rem; font-weight:800; white-space:nowrap; text-align:right;
            max-width:160px; overflow:hidden; text-overflow:ellipsis;
            background:linear-gradient(90deg,#059669,#10b981);
            -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text;
        }
        .iklan-baris-wa{
            grid-area:wa;
            display:inline-flex; align-items:center; gap:.3rem;
            background:linear-gradient(135deg,#059669,#10b981); color:#fff;
            border-radius:9px; font-size:.74rem; font-weight:800; padding:.42rem .9rem;
            text-decoration:none; box-shadow:0 3px 10px rgba(5,150,105,.3); transition:all .18s;
            white-space:nowrap;
        }
        .iklan-baris-wa:hover{color:#fff; transform:translateY(-1px);}
        .iklan-baris-wa--detail{background:#fff; color:var(--em-dark); border:1px solid var(--em-light); box-shadow:none;}
        .iklan-baris-wa--detail:hover{color:var(--em-dark); background:var(--em-xlight);}
        @media(max-width:576px){
            .iklan-baris-row{grid-template-columns:minmax(0,1fr) auto; grid-template-areas:"main wa" "price price"; column-gap:.6rem; row-gap:.35rem;}
            .iklan-baris-price{text-align:left; font-size:.85rem;}
            .iklan-baris-desc, .iklan-baris-meta{display:none;}
        }

        /* ── Empty state ── */
        .empty-state{text-align:center; padding:4rem 1rem;}
        .empty-state .empty-icon{
            width:80px; height:80px;
            background:linear-gradient(135deg,var(--em-xlight),#a7f3d0);
            border-radius:50%; display:grid; place-items:center;
            margin:0 auto 1rem; box-shadow:0 6px 18px rgba(5,150,105,.18);
        }
        .empty-state .empty-icon i{font-size:2.2rem; color:var(--em);}
        .empty-state h5{color:var(--ink); font-weight:800;}
        .empty-state p{color:var(--muted); font-size:.88rem;}

        /* ── Load-more ── */
        .load-more{
            display:inline-flex; align-items:center; gap:.45rem;
            background:#fff; border:1px solid var(--hair); border-radius:12px;
            padding:.6rem 1.9rem; font-size:.86rem; font-weight:800; color:var(--ink-soft);
            cursor:pointer; transition:all .18s;
        }
        .load-more:hover{border-color:var(--em); color:var(--em); transform:translateY(-1px);}
        .load-more:disabled{opacity:.65; cursor:wait;}

        /* ── Pagination ── */
        .pagination .page-link{color:var(--em); border-color:var(--hair); font-weight:700;}
        .pagination .page-item.active .page-link{background:linear-gradient(135deg,#059669,#10b981); border-color:var(--em); color:#fff; box-shadow:0 2px 8px rgba(5,150,105,.4);}
        .pagination .page-link:hover{background:var(--em-xlight); color:var(--em-dark); border-color:var(--em-light);}

        /* ── Footer ── */
        .site-foot{
            background:linear-gradient(135deg,#022c22,#064e3b);
            padding:2.5rem 0; margin-top:4rem; text-align:center; color:rgba(255,255,255,.75);
        }
        .site-foot .foot-title{color:#fff; font-weight:800;}
        .site-foot .foot-title span{color:#6ee7b7;}
        .site-foot p{font-size:.82rem; margin:.35rem 0 0;}
        .site-foot a{color:#6ee7b7; text-decoration:none; font-weight:700;}
        .site-foot a:hover{text-decoration:underline;}

        /* animations */
        @keyframes fadeUp{from{opacity:0; transform:translateY(10px);} to{opacity:1; transform:translateY(0);}}
        .fadeup{animation:fadeUp .6s ease both;}
        .fadeup.d1{animation-delay:.08s;}
        .fadeup.d2{animation-delay:.16s;}
    </style>
</head>
<body>

{{-- ── Floating Nav ─────────────────────────────────── --}}
<div class="nav-wrap">
    <div class="container container-xl">
        <nav class="nav-bar" aria-label="Primary">
            <a class="brand" href="{{ url('/') }}">
                <div class="brand-logo"><i class="bi bi-shop-window"></i></div>
                <div>
                    <div class="brand-name">Marketplace<span>Jamaah</span></div>
                    <div class="brand-sub">{{ $tr('brand_sub') }}</div>
                </div>
            </a>
            <div class="nav-actions">
                <div class="lang-pill" role="group" aria-label="Language">
                    <a href="{{ $langToggleUrl('id') }}" class="{{ $locale==='id'?'on':'' }}">ID</a>
                    <a href="{{ $langToggleUrl('en') }}" class="{{ $locale==='en'?'on':'' }}">EN</a>
                </div>
                <a href="{{ route('panduan') }}" class="btn-ghost d-none d-sm-inline-flex"><i class="bi bi-book"></i> {{ $tr('nav_guide') }}</a>
                <a href="{{ url('/fitur') }}" class="btn-ghost d-none d-md-inline-flex"><i class="bi bi-stars"></i> {{ $tr('nav_feat') }}</a>
                @auth
                    <a href="{{ route('dashboard') }}" class="btn-solid"><i class="bi bi-speedometer2"></i> {{ $tr('nav_dash') }}</a>
                @endauth
            </div>
        </nav>
    </div>
</div>

{{-- ── Hero ─────────────────────────────────────────── --}}
<section class="hero">
    <div class="container container-xl">
        <div class="hero-grid">
            <div class="fadeup">
                <span class="hero-eyebrow"><i class="bi bi-robot"></i> {{ $tr('hero_eyebrow') }}</span>
                <h1>{{ $tr('hero_h1_a') }} <em>{{ $tr('hero_h1_b') }}</em> {{ $tr('hero_h1_c') }}</h1>
                <p class="lead">{{ $tr('hero_lead') }}</p>
                <div class="hero-stats">
                    <span class="hero-chip"><i class="bi bi-tag-fill"></i> {{ $totalActive }} {{ $tr('stat_active') }}</span>
                    <span class="hero-chip"><i class="bi bi-grid-fill"></i> {{ $categories->count() }} {{ $tr('stat_cat') }}</span>
                    <span class="hero-chip"><i class="bi bi-whatsapp"></i> {{ $tr('stat_wa') }}</span>
                </div>
                <div class="hero-ctas">
                    <a href="#produk" class="btn-solid"><i class="bi bi-grid-fill"></i> {{ $tr('cta_browse') }}</a>
                    <a href="https://chat.whatsapp.com/F2SA2usTXXSFXYgJyvcX3k" target="_blank" rel="noopener noreferrer" class="btn-ghost" style="background:#f0fdf4; border-color:#bbf7d0; color:var(--em-dark);"><i class="bi bi-person-plus-fill"></i> {{ $tr('cta_join') }}</a>
                </div>
            </div>

            {{-- Hero stage: floating product cards from real data --}}
            <div class="hero-stage fadeup d2">
                @php
                    $heroPrimary = $heroListings->first();
                    $heroSecondary = $heroListings->skip(1)->first();

                    $buildWa = function($listing) use ($t, $locale) {
                        if (!$listing) return null;
                        $p = preg_replace('/\D/', '', $listing->contact?->phone_number ?? '');
                        if (str_starts_with($p, '0')) $p = '62' . substr($p, 1);
                        elseif ($p && !str_starts_with($p, '62')) $p = '62' . $p;
                        if (strlen($p) >= 15 && str_starts_with($p, '2500')) return null;
                        if (!$p) return null;
                        $txt = urlencode(sprintf($t['whatsapp_seller_msg'][$locale] ?? $t['whatsapp_seller_msg']['id'], $listing->title));
                        return 'https://wa.me/' . $p . '?text=' . $txt;
                    };
                    $isVid = fn($l) => $l && !empty($l->media_urls[0]) && preg_match('/\.(mp4|mov|webm)$/i', $l->media_urls[0]);
                @endphp

                @if($heroSecondary)
                    <div class="hero-card alt" aria-hidden="true">
                        @if($isVid($heroSecondary))
                            <video class="media" autoplay muted loop playsinline><source src="{{ $heroSecondary->media_urls[0] }}" type="video/mp4"></video>
                        @elseif(!empty($heroSecondary->media_urls[0]))
                            <img class="media" src="{{ $heroSecondary->media_urls[0] }}" alt="" loading="lazy">
                        @else
                            <div class="media" style="display:grid; place-items:center;"><i class="bi bi-box-seam" style="font-size:2.6rem; color:var(--em-mid);"></i></div>
                        @endif
                        <div class="body">
                            @if($heroSecondary->category)<span class="cat">{{ $heroSecondary->category->name }}</span>@endif
                            <h4>{{ $heroSecondary->title }}</h4>
                            <div class="price">{{ $heroSecondary->price_formatted ?? $tr('hero_price_nego') }}</div>
                        </div>
                    </div>
                @endif

                @if($heroPrimary)
                    <div class="hero-card main">
                        <span class="hero-pulse">{{ $locale==='en'?'Live':'Live' }}</span>
                        @if($isVid($heroPrimary))
                            <video class="media" autoplay muted loop playsinline><source src="{{ $heroPrimary->media_urls[0] }}" type="video/mp4"></video>
                        @elseif(!empty($heroPrimary->media_urls[0]))
                            <img class="media" src="{{ $heroPrimary->media_urls[0] }}" alt="{{ $heroPrimary->title }}" loading="eager">
                        @else
                            <div class="media" style="display:grid; place-items:center;"><i class="bi bi-box-seam" style="font-size:3rem; color:var(--em-mid);"></i></div>
                        @endif
                        <div class="body">
                            @if($heroPrimary->category)<span class="cat">{{ $heroPrimary->category->name }}</span>@endif
                            <h4>{{ $heroPrimary->title }}</h4>
                            <div class="price">{{ $heroPrimary->price_formatted ?? $tr('hero_price_nego') }}</div>
                            <div class="meta"><i class="bi bi-person-circle" style="color:var(--em);"></i> {{ $heroPrimary->contact?->name ?: ($heroPrimary->contact_name ?: $tr('seller_word')) }}</div>
                            @php $wa = $buildWa($heroPrimary); @endphp
                            @if($wa)
                                <a href="{{ $wa }}" target="_blank" rel="noopener" class="wa"><i class="bi bi-whatsapp"></i> {{ $tr('hero_contact') }}</a>
                            @else
                                <a href="/p/{{ $heroPrimary->id }}" class="wa"><i class="bi bi-arrow-right"></i> {{ $locale==='en'?'View Detail':'Lihat Detail' }}</a>
                            @endif
                        </div>
                    </div>
                @else
                    {{-- fallback hero card when no listings --}}
                    <div class="hero-card main" style="text-align:center; padding:2rem 1.5rem;">
                        <i class="bi bi-whatsapp" style="font-size:3rem; color:var(--em);"></i>
                        <h4 style="margin-top:1rem;">{{ $locale==='en'?'Your listings arrive via WhatsApp':'Iklanmu masuk lewat WhatsApp' }}</h4>
                        <p class="meta" style="margin-top:.5rem;">{{ $locale==='en'?'Post in the group — AI does the rest':'Posting di grup — AI yang urus sisanya' }}</p>
                    </div>
                @endif
            </div>
        </div>
    </div>
</section>

<div class="container container-xl" id="produk">

    {{-- ── Search & filters ── --}}
    <div class="search-card mb-4">
        <form method="GET" action="{{ url('/') }}">
            <input type="hidden" name="lang" value="{{ $locale }}">
            <div class="row g-2 align-items-center">
                <div class="col-12 col-md">
                    <div class="input-group">
                        <span class="input-group-text bg-white" style="border-color:#e5e7eb; border-right:none;">
                            <i class="bi bi-search" style="color:#9ca3af;"></i>
                        </span>
                        <input type="text" name="search" class="form-control" placeholder="{{ $tr('search_ph') }}"
                            value="{{ request('search') }}"
                            style="border-color:#e5e7eb; border-left:none; font-size:.92rem; color:var(--ink);">
                    </div>
                </div>
                <div class="col-12 col-md-auto d-flex gap-2">
                    <button type="submit" class="btn-search flex-fill">
                        <i class="bi bi-search"></i> {{ $tr('btn_search') }}
                    </button>
                    <button type="button" class="btn-reset" data-bs-toggle="collapse" data-bs-target="#filterAdvanced"
                            title="{{ $tr('btn_filter_hint') }}"
                            style="{{ request()->hasAny(['min_price','max_price','location']) ? 'color:var(--em); border-color:var(--em);' : '' }}">
                        <i class="bi bi-sliders"></i>
                    </button>
                    @if(request()->hasAny(['search','category_id','min_price','max_price','location']))
                        <a href="{{ url('/') }}?lang={{ $locale }}" class="btn-reset" title="{{ $tr('btn_reset') }}">
                            <i class="bi bi-x-lg"></i>
                        </a>
                    @endif
                </div>
            </div>

            <div class="collapse {{ request()->hasAny(['min_price','max_price','location']) ? 'show' : '' }}" id="filterAdvanced">
                <div class="row g-2 mt-2">
                    <div class="col-6 col-md-3">
                        <label class="form-label" style="font-size:.78rem; color:var(--muted); font-weight:700; margin-bottom:.25rem;">{{ $tr('filter_minp') }}</label>
                        <input type="number" name="min_price" class="form-control form-control-sm" placeholder="50000"
                               value="{{ request('min_price') }}" min="0" step="1000"
                               style="border-color:#e5e7eb; font-size:.88rem;">
                    </div>
                    <div class="col-6 col-md-3">
                        <label class="form-label" style="font-size:.78rem; color:var(--muted); font-weight:700; margin-bottom:.25rem;">{{ $tr('filter_maxp') }}</label>
                        <input type="number" name="max_price" class="form-control form-control-sm" placeholder="500000"
                               value="{{ request('max_price') }}" min="0" step="1000"
                               style="border-color:#e5e7eb; font-size:.88rem;">
                    </div>
                    <div class="col-12 col-md-6">
                        <label class="form-label" style="font-size:.78rem; color:var(--muted); font-weight:700; margin-bottom:.25rem;">{{ $tr('filter_loc') }}</label>
                        <div class="input-group input-group-sm">
                            <span class="input-group-text bg-white" style="border-color:#e5e7eb;">
                                <i class="bi bi-geo-alt" style="color:#9ca3af;"></i>
                            </span>
                            <input type="text" name="location" class="form-control" placeholder="{{ $tr('filter_loc_ph') }}"
                                   value="{{ request('location') }}"
                                   style="border-color:#e5e7eb; font-size:.88rem;">
                        </div>
                    </div>
                </div>
            </div>

            @php
                $qBase = array_filter(request()->only(['search','min_price','max_price','location']));
                $qBase['lang'] = $locale;
            @endphp
            <div class="cat-pills-wrap mt-3">
                <a href="{{ url('/') }}?{{ http_build_query($qBase) }}"
                   class="cat-pill {{ !request('category_id') ? 'active' : '' }}">
                    <i class="bi bi-grid"></i> {{ $tr('cat_all') }}
                </a>
                @foreach($categories as $cat)
                    <a href="{{ url('/') }}?{{ http_build_query(array_merge($qBase, ['category_id' => $cat->id])) }}"
                       class="cat-pill {{ request('category_id') == $cat->id ? 'active' : '' }}">
                        {{ $cat->name }}
                    </a>
                @endforeach
            </div>
        </form>
    </div>

    {{-- ── Trust strip ── --}}
    <div class="trust-strip mb-4">
        <div class="trust-row">
            <div class="trust-item"><i class="bi bi-shield-check-fill"></i>{{ $tr('trust_verified') }}</div>
            <div class="trust-item"><i class="bi bi-whatsapp"></i>{{ $tr('trust_wa') }}</div>
            <div class="trust-item"><i class="bi bi-robot"></i>{{ $tr('trust_ai') }}</div>
            <div class="trust-item"><i class="bi bi-people-fill"></i>{{ $tr('trust_comm') }}</div>
        </div>
    </div>

    {{-- ── Top categories ── --}}
    @if($topCategories->isNotEmpty())
    <div class="mb-4">
        <div class="topcat-head">
            <h6><i class="bi bi-bar-chart-fill"></i> {{ $tr('top_cat_title') }}</h6>
            <span class="muted">{{ $tr('top_cat_sub') }}</span>
        </div>
        <div class="topcat-row">
            @foreach($topCategories as $i => $tc)
                @php
                    $rank = $i + 1;
                    $palette = ['#dcfce7','#d1fae5','#a7f3d0','#6ee7b7','#34d399'];
                    $bg = $palette[$i] ?? '#f3f4f6';
                @endphp
                <a href="{{ url('/') }}?{{ http_build_query(array_merge($qBase, ['category_id' => $tc->id])) }}"
                   class="topcat-item" style="background:{{ $bg }};">
                    <span class="rank">#{{ $rank }}</span>
                    {{ $tc->name }}
                    <span class="cnt">{{ $tc->listings_count }}</span>
                </a>
            @endforeach
        </div>
    </div>
    @endif

    {{-- ── WhatsApp join banner ── --}}
    <div class="wa-banner mb-4">
        <div class="wa-inner">
            <div class="wa-qr">
                <img src="https://api.qrserver.com/v1/create-qr-code/?size=140x140&data=https%3A%2F%2Fchat.whatsapp.com%2FF2SA2usTXXSFXYgJyvcX3k&bgcolor=ffffff&color=064e3b&margin=5"
                     alt="QR WhatsApp Group" width="110" height="110" loading="lazy"
                     style="display:block; border-radius:6px;">
            </div>
            <div>
                <div class="wa-lbl"><i class="bi bi-whatsapp"></i> {{ $tr('wa_label') }}</div>
                <h3 class="wa-title">{{ $tr('wa_title') }}</h3>
                <p class="wa-desc">{{ $tr('wa_desc') }}</p>
                <a href="https://chat.whatsapp.com/F2SA2usTXXSFXYgJyvcX3k" target="_blank" rel="noopener noreferrer" class="wa-btn">
                    <i class="bi bi-whatsapp" style="font-size:1.1rem;"></i> {{ $tr('wa_btn') }}
                </a>
            </div>
        </div>
    </div>

    {{-- ── Moderation notice ── --}}
    <div class="mod-notice mb-3">
        <span class="ico">⏳</span>
        <div class="body"><strong>{{ $tr('mod_title') }}</strong> {{ $tr('mod_body') }}</div>
    </div>

    {{-- ── Listings ── --}}
    @if($listings->isEmpty() && $textListings->isEmpty())
        <div class="empty-state">
            <div class="empty-icon"><i class="bi bi-bag-x"></i></div>
            <h5>{{ $tr('empty_title') }}</h5>
            <p>{{ $tr('empty_sub') }}</p>
            <a href="{{ url('/') }}?lang={{ $locale }}" class="btn-search" style="margin-top:.6rem;">
                <i class="bi bi-arrow-left"></i> {{ $tr('empty_btn') }}
            </a>
        </div>
    @else

        @if($listings->isNotEmpty())
        <div class="section-divider">
            <h3><i class="bi bi-image-fill"></i> {{ $tr('section_media_title') }} <span class="count">· {{ $listings->total() }} {{ $tr('section_media_sub') }}</span></h3>
            <div class="line"></div>
        </div>

        <div class="row row-cols-2 row-cols-sm-3 row-cols-lg-3 g-3 mb-2" id="productGrid">
            @include('landing._cards', ['listings' => $listings->items()])
        </div>

        @if($listings->hasMorePages())
        <div id="loadMoreMediaWrap" style="text-align:center; margin:1.4rem 0 1.5rem;">
            <button id="loadMoreMediaBtn" onclick="loadMoreMedia()" class="load-more">
                <i class="bi bi-arrow-down-circle"></i> {{ $tr('load_more_media') }}
            </button>
        </div>
        @endif
        @endif

        @if($textListings->isNotEmpty())
        <div class="section-divider" style="margin-top:3rem;">
            <h3><i class="bi bi-list-ul"></i> {{ $tr('section_text_title') }} <span class="count">· {{ $textListings->total() }} {{ $tr('section_text_sub') }}</span></h3>
            <div class="line"></div>
        </div>
        <p style="font-size:.82rem; color:var(--muted); margin:-.2rem 0 1rem;">{{ $tr('section_text_lead') }}</p>

        <div id="iklanBarisList" class="iklan-baris-list mb-3">
            @include('landing._iklan_baris', ['listings' => $textListings->items()])
        </div>

        @if($textListings->hasMorePages())
        <div id="loadMoreTextWrap" style="text-align:center; margin-bottom:2rem;">
            <button id="loadMoreTextBtn" onclick="loadMoreText()" class="load-more">
                <i class="bi bi-arrow-down-circle"></i> {{ $tr('load_more_text') }}
            </button>
        </div>
        @endif
        @endif

    @endif
</div>

{{-- ── Footer ── --}}
<footer class="site-foot">
    <div class="container container-xl">
        <div class="foot-title">Marketplace<span>Jamaah</span> AI</div>
        <p>{{ $tr('footer_tag') }} &middot;
            <a href="{{ url('/fitur') }}">{{ $tr('nav_feat') }}</a> &middot;
            <a href="{{ route('panduan') }}">{{ $tr('nav_guide') }}</a> &middot;
            <a href="{{ $langToggleUrl($locale==='en'?'id':'en') }}">{{ $locale==='en'?'Bahasa Indonesia':'English' }}</a>
        </p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
(function() {
    var _LOAD_MORE_MEDIA = @json($tr('load_more_media'));
    var _LOAD_MORE_TEXT  = @json($tr('load_more_text'));
    var _LOADING         = @json($tr('loading'));

    var _page    = 1;
    var _loading = false;
    var _search   = @json(request('search', ''));
    var _catId    = @json(request('category_id', ''));
    var _minPrice = @json(request('min_price', ''));
    var _maxPrice = @json(request('max_price', ''));
    var _location = @json(request('location', ''));
    var _grid    = document.getElementById('productGrid');

    function _buildParams(page, type) {
        var p = new URLSearchParams({ page: page, type: type });
        if (_search)   p.append('search',      _search);
        if (_catId)    p.append('category_id', _catId);
        if (_minPrice) p.append('min_price',   _minPrice);
        if (_maxPrice) p.append('max_price',   _maxPrice);
        if (_location) p.append('location',    _location);
        return p;
    }

    window.loadMoreMedia = function() {
        if (_loading) return;
        var btn = document.getElementById('loadMoreMediaBtn');
        _loading = true;
        if (btn) { btn.disabled = true; btn.innerHTML = '<span class="spinner-border spinner-border-sm me-1" role="status"></span>' + _LOADING; }
        _page++;
        var p = _buildParams(_page, 'media');
        fetch('/produk/lagi?' + p.toString(), { headers: { 'X-Requested-With': 'XMLHttpRequest' } })
            .then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); })
            .then(function(d) {
                if (_grid) _grid.insertAdjacentHTML('beforeend', d.html);
                if (!d.hasMore) {
                    var wrap = document.getElementById('loadMoreMediaWrap');
                    if (wrap) wrap.style.display = 'none';
                } else if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="bi bi-arrow-down-circle"></i> ' + _LOAD_MORE_MEDIA;
                }
                _initVideoAutoplay();
            })
            .catch(function(e) {
                console.error('load-more-media:', e); _page--;
                if (btn) { btn.disabled = false; btn.innerHTML = '<i class="bi bi-arrow-down-circle"></i> ' + _LOAD_MORE_MEDIA; }
            })
            .finally(function() { _loading = false; });
    };

    var _textPage    = 1;
    var _textLoading = false;
    window.loadMoreText = function () {
        if (_textLoading) return;
        var btn = document.getElementById('loadMoreTextBtn');
        _textLoading = true;
        if (btn) { btn.disabled = true; btn.innerHTML = '<span class="spinner-border spinner-border-sm me-1" role="status"></span>' + _LOADING; }
        _textPage++;
        var p2 = _buildParams(_textPage, 'text');
        fetch('/produk/lagi?' + p2.toString(), { headers: { 'X-Requested-With': 'XMLHttpRequest' } })
            .then(function(r) { if (!r.ok) throw new Error(r.status); return r.json(); })
            .then(function(d) {
                var list = document.getElementById('iklanBarisList');
                if (list) list.insertAdjacentHTML('beforeend', d.html);
                if (!d.hasMore) {
                    var wrap = document.getElementById('loadMoreTextWrap');
                    if (wrap) wrap.style.display = 'none';
                } else if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="bi bi-arrow-down-circle"></i> ' + _LOAD_MORE_TEXT;
                }
            })
            .catch(function(e) {
                console.error('load-more-text:', e); _textPage--;
                if (btn) { btn.disabled = false; btn.innerHTML = '<i class="bi bi-arrow-down-circle"></i> ' + _LOAD_MORE_TEXT; }
            })
            .finally(function() { _textLoading = false; });
    };

    function _initVideoAutoplay() {
        if (!('IntersectionObserver' in window)) return;
        document.querySelectorAll('.card-autoplay-video:not([data-observed])').forEach(function(vid) {
            vid.setAttribute('data-observed', '1');
            var autoObs = new IntersectionObserver(function(entries) {
                entries.forEach(function(entry) {
                    if (entry.isIntersecting) {
                        if (!vid.src && vid.dataset.src) vid.src = vid.dataset.src;
                        vid.play().catch(function(){});
                    } else {
                        vid.pause();
                    }
                });
            }, { threshold: 0.25 });
            autoObs.observe(vid);
        });
    }
    _initVideoAutoplay();
})();
</script>
</body>
</html>
